<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/_document_head') %>

  <title>URL - TinyApp Example</title>
</head>

<body>
  <%- include('partials/_header') %>
  <div class="card text-center">
      <div class="card-header"></div>
      <div class="card-body">
        <h5 class="card-title">TinyURL for: <%= longURL %></h5>
        <p class="card-text">Short URL: <a href="/u/<%= shortURL %>"><%= shortURL %></a></p>
      </div>
    <div class="card-footer text-muted"></div>
    <form method="POST" action="/urls/<%= shortURL %>?_method=PUT">
      <div>
        <label>Edit:</label>
          <input class="" type="text" name="longURL" placeholder="http://" style="width: 240px; margin: 1em">
          <button type="submit" class="btn btn-outline-success">Submit</button>
      </div>
   </form>
  </div>
  <!-- Bootstrap JS -->
  <%- include('partials/_scripts') %>
</body>
</html>